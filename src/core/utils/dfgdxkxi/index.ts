import {CreateComputedOptions, EffectCleanupFn, Signal} from '@angular/core';

// todo: should work without injection context

export const dfgdxkxi: {
	<T>(
		//
		fn: {(onCleanup: EffectCleanupFn): Signal<T>},
		options?: CreateComputedOptions<T>,
	): Signal<T>;
} = (fn, options) => {
	// todo
	throw '';
};
