import {signal} from '@angular/core';

export const dbeqzuvj: {
	(): {
		track(): void;
		trigger(): void;
	};
} = () => {
	let kyrpsrpf = signal({});
	return {
		track: () => kyrpsrpf(),
		trigger: () => kyrpsrpf.set({}),
	};
};
